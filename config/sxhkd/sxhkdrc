super + q
             /home/tawargy/bin/exit.sh

super + shift + r             
            rofi -show drun
            
super + s 
            slock
            
super +  shift + enter
            kitty

alt + ctrl + i
            st -e "nmtui" 

alt + ctrl + p             
            pavucontrol

alt + ctrl + m             
            arandr

alt + ctrl + h
            st -e htop


alt + f
            pcmanfm
alt + r
            st -e ranger
alt + m
            audacious
alt + w 
            brave-browser
alt + o
            flatpak run md.obsidian.Obsidian
alt + z   
            python3 /home/tawargy/bin/moazin.py

alt + 1
            light -U 5
alt + 2            
            light -A 5

alt + equal
            pactl set-sink-volume @DEFAULT_SINK@ +5% 
alt + minus
            pactl set-sink-volume @DEFAULT_SINK@ -5%
alt + 0
            pactl set-sink-mute @DEFAULT_SINK@ toggle


alt + >
            mpv $(xclip -o)

